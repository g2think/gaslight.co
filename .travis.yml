language: ruby
rvm:
  - "1.9.3"
bundler_args: --local --quiet --binstubs --path vendor --without production
script:
  - bin/rspec -fd spec
  - bin/cucumber
  - bin/rake konacha:run
before_script:
  - cp .env.sample .env.test
  - cp config/database.sample.yml config/database.yml
